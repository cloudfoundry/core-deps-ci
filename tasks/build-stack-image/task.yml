---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: vito/oci-build-task
inputs:
  - name: stacks
run:
  path: build

outputs:
  - name: image

params:
  CONTEXT:
  BUILD_ARGS_FILE:
